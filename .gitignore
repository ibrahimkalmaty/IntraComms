# Virtual environments
venv/
.venv/

# Python cache
__pycache__/
*.pyc

# Database (regenerate with: flask --app server/server.py init-db)
intracomms.db
intracomms.db.bak

# Secrets & TLS keys — never commit these
.env
key.pem
cert.pem

# User-uploaded files
uploads/*
!uploads/.gitkeep

# Local junk
tmp_wheels/
.claude/settings.local.json
