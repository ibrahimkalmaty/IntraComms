# IntraComms - Python dependencies
# Install with: pip install -r requirements.txt

# Web framework
Flask==3.1.1
Flask-SocketIO==5.5.1          # real-time messaging over WebSockets
Flask-SQLAlchemy==3.1.1        # ORM for SQLite
Flask-Login==0.6.3             # session management / login_required decorator

# WebSocket transport
eventlet==0.39.1               # async worker required by Flask-SocketIO

# Database
SQLAlchemy==2.0.41

# Encryption
cryptography==44.0.3           # RSA key generation, AES-256 encrypt/decrypt

# File handling
Werkzeug==3.1.3                # FileStorage, secure_filename (bundled with Flask)
python-magic==0.4.27           # MIME type detection for uploaded files

# Media processing
# NOTE (MSYS2/MinGW Python): no pre-built wheel — build from source:
#   pacman -S mingw-w64-x86_64-zlib mingw-w64-x86_64-libjpeg-turbo mingw-w64-x86_64-libpng mingw-w64-x86_64-libwebp
#   pip install Pillow  (builds from source; app runs without it — graceful fallback to raw image storage)
Pillow>=10.0.0                 # image re-encode to WebP, EXIF strip, thumbnail generation
APScheduler>=3.10.0            # background cleanup of abandoned chunk uploads

# Utilities
python-dotenv==1.1.0           # loads .env into os.environ
